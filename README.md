## HyperPatch Patch List（补丁列表）

1. Disable Rescue Party Plus（禁用 Rescue Party Plus，防止应用闪退概率被清空数据）by Sevtinge
2. Context-Aware ADB Toggle（基于连接状态自动切换 ADB）by Meltartica
3. Hide HTTP Services, Whitelist/Blacklist Mode, TCP/UDP + CIDR Support（本地 HTTP 服务隐藏，白名单/黑名单模式，支持 TCP/UDP 与 CIDR）by Meltartica, 酷安@ATK队友z
4. Force Enable 1Hz Refresh Rate for AOD（强制启用 AOD 的 1Hz 刷新率），需要搭配全屏AOD by 酷安@domexie
5. Clear Volume Level Blacklist（清空音量黑名单列表）by 后宫学长, ⼼劫

## Optional（可选项）

1. Disable Preloading Applications（禁用应用预加载）
